<div class="container">

  <p id="notice"><%= notice %></p>
  
  <div class="row">
    <h2>Season</h2>
  </div>
  
  <div class="row even-row">
    <div class="col-xs-9 vcenter">
      <p><%= @season.name %></p>
    </div>
    <div class="col-xs-1 padding-top padding-bottom">
      <%= link_to 'Edit', edit_season_path(@season), class: 'btn btn-primary outline' %>
    </div>
  </div>
  
  <div class="row padding">
    <%= link_to 'Back to Seasons', show_league_path(@season.league), class: 'btn btn-primary outline' %>
  </div>
  
  <div class="row padding">
    <h2>Teams</h2>
    <div class="col-xs-9 vcenter"
      <h2>Teams</h2>
    </div>
    <div class="col-xs-1 padding-top padding-botom">
      <%= link_to 'Add Team', add_team_path(@season), class: 'btn btn-primary outline' %>
    </div>
  </div>
  
  <%= render 'teams/index' %>

  <div class="row padding">  
    <%= link_to 'Add Team', add_team_path(@season), class: 'btn btn-primary outline' %>
  </div>
</div>
