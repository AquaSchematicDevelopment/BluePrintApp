<div class="container">
  
  <h2>Player List</h2>
  <% @players.each do |player| %>
  <div class="">
    <span class="row">
      <label>Username</label>
      <p><%= player.name %></p>
      <%= link_to 'Delete', delete_player_path(player.id), method: 'delete', class: 'btn btn-primary' %>
    </span>
  </div>
  <% end %>

  <%= link_to 'Register Player', new_player_path, class: 'btn btn-primary' %>

</div>
