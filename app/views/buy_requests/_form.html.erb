<%= form_for(@buy_request) do |f| %>
  <% if @buy_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@buy_request.errors.count, "error") %> prohibited this buy_request from being saved:</h2>

      <ul>
      <% @buy_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
