<div class="row padding">
  <div class="col-xs-3">
    <p><b>Team</p></b>
  </div>
  <div class="col-xs-2">
    <center><p><b>Book Value</p></b></center>
  </div>
  <div class="col-xs-2">
    <center><p><b>Market Price</p></b></center>
  </div>
</div>

<% @teams.each_with_index do |team, index| %>
<div class="row <%= index.even? ? 'even-row' : 'odd-row' %>">
  <div class="col-xs-3 vcenter">
    <p><%= team.name %></p>
  </div>
  <div class="col-xs-2 vcenter">
    <center><p><%= team.book_value %></p></center>
  </div>
  <div class="col-xs-2 vcenter">
    <center><p><%= team.market_price_formated %></p></center>
  </div>
  <div class="col-xs-2 vcenter">
    <center><p><%= team.sell_requests.empty? ? 'No Players Selling' : 'Currently Selling' %></p></center>
  </div>
  <div class="col-xs-2 col-xs-offset-1 padding-top padding-bottom">
    <%= link_to 'buy', team_sell_requests_path(team), class: 'btn btn-primary outline' %>
  </div>
</div>
<% end %>
