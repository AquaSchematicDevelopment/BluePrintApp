<div class="row padding">
  <div class="col-xs-5">
    <p><b>Price</b></p>
  </div>
  <div class="col-xs-5">
    <p><b>Amount</b></p>
  </div>
</div>

<% @sell_requests.each_with_index do |sell_request, index| %>
  <div class="row <%= index.even? ? 'even-row' : 'odd-row' %>">  
    <div class="col-xs-5 vcenter">
      <p><%= sell_request.price %></p>
    </div>
    <div class="col-xs-5 vcenter">
      <p><%= sell_request.amount %></p>
    </div>
    <div class="col-xs-2 padding-top padding-bottom">
      <%= link_to 'Buy', buy_sell_request_path(sell_request), class: 'btn btn-primary outline' %>
    </div>
  </div>
<% end %>

